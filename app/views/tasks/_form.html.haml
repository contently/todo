
-if @task.errors.any?
  #error_explanation
    %h2="#{pluralize(@task.errors.count, "error")} prohibited this task from being saved:"
    %ul
      -@task.errors.full_messages.each do |msg|
        %li= msg

.field
  =f.label :name
  =f.text_field :name

-# HW1: hide checkbox on create
-unless @task.new_record?
  .field
    =f.label :completed
    =f.check_box :completed

.actions
  =f.submit 'Save'

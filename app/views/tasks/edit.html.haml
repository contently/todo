%h1= @list.name
=render 'form'

%br

%p
  %b Audit Log

%table
  %thead
    %tr
      %th Action
      %th Time

  %tbody
    - @task.task_changes.each do |task_change|
      %tr
        %td=task_change.action
        %td=task_change.created_at.strftime("%-m/%d/%Y at %l:%M:%S %p UTC")

%br
